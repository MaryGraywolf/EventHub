apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: backend
  labels:
    app: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - port: ${BACKEND_PORT}
    targetPort: ${BACKEND_PORT}
    nodePort: 30081
    protocol: TCP
    name: http
